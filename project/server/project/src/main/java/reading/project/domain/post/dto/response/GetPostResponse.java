package reading.project.domain.post.dto.response;

public class GetPostResponse {
    /*
    * Member Info (nickname, username, followStatus, isAuthor)
    * Book Info (title, author, image)
    * ReadingPlan Info (startDate, endDate)
    * Post Info (title, content)
    * Comment Info (nickname, username, content, countComment)
    * countLike
    * */

}
